@using Models.Metadata

@inherits TemplateBase
@attribute [Multiline]
@attribute [ReadOnly]
@attribute [ValueKind(typeof(string))]
<div class=row>
	<div class=col-12>
		@Label:
		<Radzen.Blazor.RadzenTextArea Disabled @bind-Value=Value></Radzen.Blazor.RadzenTextArea>
	</div>
</div>

@code {
	[Parameter]
	public string? Value { get; set; }
}
