@using Models.Metadata

@inherits TemplateBase
@attribute [ValueKind(typeof(double))]

@Label:
<Radzen.Blazor.RadzenNumeric Disabled TValue="double" @bind-Value="Value"></Radzen.Blazor.RadzenNumeric>

@code {
	[Parameter]
	public double Value { get; set; }
}
