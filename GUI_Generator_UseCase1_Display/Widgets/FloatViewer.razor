@using GeneratorSharedComponents.Abstractions
@using Models.Metadata

@inherits WidgetBase
@attribute [ValueKind(typeof(float))]

<Radzen.Blazor.RadzenNumeric Disabled Min="0" Max="10" @bind-Value="Value"></Radzen.Blazor.RadzenNumeric>
@code {
	[Parameter]
	public float Value { get; set; }
}
